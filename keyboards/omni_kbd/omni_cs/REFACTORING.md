# omni_cs リファクタリング調査・計画

## 目的と制約

`omni_kbd/omni_cs` の可読性と保守性を、既存のキーボード動作を維持したまま改善する。
変更は `keyboards/omni_kbd` 内に限定し、挙動変更が必要な事項は実装前に確認する。

## 現在の構成

- `omni_cs/omni_cs.c`: QMK のキーボードフックを起点に、起動、LCD、2 個の
  PMW3360、スリープ、独自キーコード、Vial キーマップ更新を統括する。
- `omni_cs/matrix.c`: 物理キーの duplex matrix と LCD 上の仮想キーを一つの
  QMK matrix として走査する。
- `common/touch_lcd_omni.c`: タッチイベントの表示モード別配送と、表示モードに応じた
  再描画の調停を扱う。
- `common/touch_input.c`: RP2040 GPIO のエッジイベントを一定間隔で集計し、タッチの
  接触開始・終了を判定する。ハードウェア依存の接触検出状態はこのモジュールが所有する。
- `common/touch_gesture.c`: 接触開始座標から押下・スワイプを判定し、単発入力、長押し、
  リピート、離脱リセットの状態遷移と、タッチ間隔設定、仮想キー押下状態を所有する。
- `common/touch_key_view.c`: タッチキー画面の画像配置、選択中カテゴリ・レイヤー、
  スワイプによる画面遷移、円形キーのヒットテストを所有する。
- `common/swipe_gesture.c`: スワイプ画面の4プロファイル、選択中プロファイル、描画、
  ジェスチャーに対応するキー送出を所有する。
- `common/status_view.c`, `view_keymap.c`, `sleeping_view.c`: ステータス画面、
  キーマップ画面、スリープ画面を描画する。
- `common/omni_layers.h`: 複数の表示モジュールが参照するキーボードレイヤー定数を定義する。
- `common/trackball_omni.c`: カーソル、スクロール、慣性スクロールの変換を行う。
- `common/trackball_tuning.c`: トラックボール調整画面の描画、タッチ領域判定、設定保存を扱う。
- `common/config_omni.c`: EEPROM 上の設定値を読み書きする。
- `drivers/`: CST816T タッチコントローラと PMW3360 の低レベルドライバ。
- `font/`, `icon/`: Quantum Painter 用の生成済みリソース。
- `icon/omni_image_loader.c`: 生成済み画像を読み込み、画像ハンドルを所有する。
- `omni_cs/rules.mk`: 上記ソースと生成済み画像を列挙するビルド定義。

## 主な処理の流れ

1. `matrix_init_user()` が LCD バックライト、I2C、タッチ割り込み入力、仮想キーを初期化する。
2. `keyboard_post_init_kb()` が EEPROM 設定、LCD、フォント、タッチ IC、画像を初期化する。
3. `matrix_scan_custom()` が物理 matrix とタッチ由来の仮想キーを走査する。
4. `pointing_device_task_kb()` が左右センサーを読み、モードに応じてカーソルまたは
   高解像度スクロールへ変換し、レイヤーに応じて表示を更新する。
5. `matrix_scan_user()` と `housekeeping_task_user()` がタッチ処理、起動画面終了、
   LCD スリープと復帰を進める。
6. `process_record_kb()` が色、トラックボールモード、表示モードの独自キーコードを処理する。
7. Vial がキーマップを書き換えた場合、リンク時の wrapper が仮想キーと設定を再読込する。

## 調査で確認した保守性上の課題

- 機能境界を越えるグローバル変数が多く、所有元と更新箇所を追いにくい。
- `omni_cs.c` が複数の責務を持ち、長い条件分岐と初期化処理が混在している。
- matrix の半分、仮想キー数、LCD 上の円数などが式や数値で繰り返されている。
- `matrix.c` には重複する GPIO 初期化、常に同じ値を返すための冗長な条件、
  未使用引数がある。
- 命名、括弧、空白、コメントの粒度がファイル内でも統一されていない。
- 生成済み画像 100 個前後が `rules.mk` に個別列挙され、変更時の確認が難しい。
- 一部の公開ヘッダーが実装詳細の状態を多数 `extern` で公開している。

## 命名方針

- 変数名と構造体メンバー名には役割が分かる名前を使い、一部例外を除き、基本的に1 文字の識別子は使用しない。
- ループ変数も `image_index`、`row_index` のように対象を名前で示す。（短縮形としてidx程度は認める）
- 純粋に座標を指すなら `x`、`y`という命名を認めるが、移動量であれば `delta_x`、`delta_y` のように明示する。
- その他例外として、明確に幅、高さを示したい場合のw,hなどや、完全に意味を持たないループカウンタとしてのi（単に10回繰り返したいだけの場合にi<10とするようなもの）などは1文字でもよいものとする。
- 外部ライブラリが定義する型やメンバー名は変更対象外とする。

## 実施計画

### フェーズ 1: 動作を変えない局所整理

- 用途が明確な定数を名前付きにし、ループと境界計算を読みやすくする。
- `omni_cs.c` の長い処理を責務別の `static` helper に分割する。
- `matrix.c` の重複初期化、未使用引数、冗長な条件を整理する。
- 各段階で `make omni_kbd/omni_cs:vial` が成功することを確認する。

### フェーズ 2: モジュール境界の整理

- 共有状態の所有元を決め、直接参照を getter/setter または状態構造体へ寄せる。
- LCD、タッチ、設定、トラックボール間の公開 API を必要最小限にする。
- `omni_cs.c` から独立可能な制御処理を `common/` の適切なモジュールへ移す。

### フェーズ 3: ビルド定義と未使用コードの整理

- 生成リソースの列挙方法を整理し、追加・削除時の手順を文書化する。
- 参照関係を確認して未使用コードを候補化する。
- 削除やタイミング変更など挙動に影響し得る変更は、候補と影響を提示して確認後に行う。

## 検証方針

- 基準ビルド: `make omni_kbd/omni_cs:vial`（調査開始時点で成功）。
- 各フェーズで同じコマンドによるコンパイル・リンク・UF2 生成を確認する。
- 実機確認では物理キー、LCD 仮想キー、左右トラックボール各モード、慣性スクロール、
  全表示モード、Vial 更新、スリープ・復帰、サスペンドを確認する。

## 進捗

- 2026-08-02: 基準ビルドの成功を確認。
- 2026-08-02: フェーズ 1 に着手。matrix と LCD 配列の名前付き定数化、matrix GPIO
  初期化の重複除去、タッチ走査の未使用引数除去、レイヤー表示・タッチフィードバック・
  起動後 2 フレーム目描画の helper 分割を実施。
- 2026-08-02: 上記変更後のコンパイル、リンク、UF2 生成の成功を確認。
- 2026-08-02: `omni_cs.c` の独自キー処理を色設定・表示モード処理へ分割し、起動処理を
  設定読込・LCD/フォント・タッチ IC・ロゴ表示へ分割。ファイル内状態と helper を
  `static` 化し、仮想キー読込用の一時配列を除去。
- 2026-08-02: matrix の座標判定 helper と仮想キー配列を非公開化し、LCD キー位置を
  読み取り専用に変更。変更後のコンパイル、リンク、UF2 生成の成功を確認。
- 2026-08-02: `touch_lcd_omni.c` の内部状態・内部 helper を `static` 化し、未使用配列、
  未使用タイマー、未呼出しのパルス計測コード、古いコメントアウト済み実装を削除。
- 2026-08-02: タッチ割り込みの状態機械を、初期化、接触開始、初回ジェスチャー判定、
  単発終了、リピート開始・実行、離脱リセットへ分割。閾値と遷移順は維持し、変更後の
  コンパイル、リンク、UF2 生成の成功を確認。
- 2026-08-02: 1 文字の変数・メンバーを避ける命名方針を追加。単一メンバーだった
  `touch_fsm_t` を廃止して `touch_contact_active` に置換し、タッチ処理と matrix の
  インデックス・座標・移動量を役割が分かる名前へ変更。
- 2026-08-02: ファイル途中にあった `hardware/gpio.h` とタッチ接触検出用マクロを整理。
  GPIO 割り込み、サンプリング窓、接触 ON/OFF 判定を `touch_input.c/.h` に分離し、
  `touch_lcd_omni.c` にはジェスチャー状態機械と表示処理を残した。変更後のコンパイル、
  リンク、UF2 生成の成功を確認。
- 2026-08-02: `touch_lcd_omni.c` と `omni_image_loader.c` に重複定義されていた画像
  ハンドルを前者から削除し、画像ローダーを唯一の所有元に変更。
- 2026-08-02: スワイプの4プロファイルについて、表示ラベル・方向別キーコード・
  修飾キー方式をテーブル化。入れ子の `switch` を共通実行処理へ置換し、Control、
  Control+GUI の押下・解放順を維持。変更後のコンパイル、リンク、UF2 生成に成功。
- 2026-08-02: 画像ローダーが公開していたロゴ、保存、レイヤー12個、キー画像255個の
  個別グローバルハンドルを非公開配列へ集約。ヘッダーを初期化関数と用途別の4取得API
  のみに縮小した。初期化前に画像ハンドルのアドレスを保持する仕様と、未生成キー画像が
  NULL ハンドルを参照する仕様は維持。変更後のコンパイル、リンク、UF2 生成に成功。
- 2026-08-02: 画像ローダーの無名 `enum` をレイヤー画像数とマクロキー範囲の概念ごとに分離。
- 2026-08-02: トラックボール調整画面を `trackball_tuning.c/.h` に分離。番号ベースだった
  座標・テキスト・ボタン名を役割ベースに変更し、描画、値更新、変更曲線の再描画を分割。
  ボタン判定順、値の増減・上限下限、保存・再描画順を維持し、変更後のフルビルドに成功。
- 2026-08-02: スワイプ画面のプロファイル状態、描画、ジェスチャー別キー送出を
  `swipe_gesture.c/.h` に分離。表示デバイス、フォント、対象レイヤー、ジェスチャーIDを
  引数で受け取るAPIとし、暗黙の共有状態への依存を削減した。既存の描画順と修飾キーの
  押下・解放順は維持した。
- 2026-08-02: レイヤー定数を `omni_layers.h` に移し、レイヤー表示名の取得を
  `view_keymap.c` に集約。型として使用しないレイヤー定数は無名 `enum` とし、型を持つ
  スワイプ修飾方式には名前付き `enum` を使用した。変更後のフルビルドに成功。
- 2026-08-02: タッチキー画面を `touch_key_view.c/.h` に分離。画像配置配列、選択中の
  カテゴリ・レイヤー、画面描画、スワイプ遷移、円形キーのヒットテストを同モジュールの
  所有とし、`matrix.c` には取得した仮想行・列を matrix に反映する処理だけを残した。
  `touch_lcd_omni.h` から画像配置型、配置配列、カテゴリ・レイヤー状態の `extern` を削除。
  初期化・描画順、座標、半径、境界条件、仮想 matrix 行の計算を維持し、変更後の
  フルビルドに成功。
- 2026-08-02: タッチの押下・スワイプ判定と、開始待ち、単発終了、長押し待ち、リピート、
  離脱リセットの状態機械を `touch_gesture.c/.h` に分離。検出結果はイベント構造体と
  コールバックで `touch_lcd_omni.c` に渡し、状態機械から各表示モジュールへの依存を
  排除した。タッチ間隔設定と仮想キーの座標・押下・フィードバック状態も同モジュールへ
  集約し、status画面とmatrixはsetter/getter経由で参照するよう変更。
- 2026-08-02: 初期値以外に設定されず到達不能だった `touch_start_flag`、
  `initial_touch_flag`、`touch_start_timer` と、その後処理および未使用マクロを削除。
  既存の判定閾値、タイマー条件、状態遷移順、イベント処理順を維持し、変更後の
  フルビルドに成功。
- 2026-08-02: 表示モードを `touch_lcd_omni.c` の非公開状態に変更し、参照・変更を
  `display_get_mode()`、`display_set_mode()` に集約。モード切替キーの重複した描画処理を
  「モード選択後に `display_redraw()`」という共通経路へ統一した。matrixの表示モード判定も
  getter経由に変更し、処理のない `switch` 分岐を単純な条件判定へ置換。
- 2026-08-02: キーボードの現在レイヤーを表示モジュールのグローバル状態から
  `omni_cs.c` の非公開状態へ移し、再描画時に明示的な引数として渡すよう変更。
  モード切替時、スリープ復帰時、Vial更新時の描画・状態更新順を維持し、変更後の
  コンパイル、リンク、UF2生成に成功。
- 2026-08-02: LCDデバイスと用途の異なる4フォントを `omni_display_context_t` にまとめ、
  `omni_cs.c` の非公開状態として所有するよう変更。各リソースは複数の描画経路で常に
  同じ組として使用されるため、表示処理へ明示的に渡すコンテキスト構造体とした。
- 2026-08-02: `touch_lcd_omni.h` が公開していたLCDデバイス・フォントの5つの `extern` を
  削除。タッチ状態機械のコールバックに任意コンテキストを渡せるようにし、タッチイベント
  配送にも表示コンテキストを明示的に引き渡した。ヘッダーから不要な設定ファイルの
  推移的includeも除去。初期化順、タッチ処理順、各画面の描画順を維持し、変更後の
  コンパイル、リンク、UF2生成に成功。
